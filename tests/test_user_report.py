import pytest
from app.models import User
from app.extensions import db

## ISSUE III Create Tests for the following
# test_user_report_all_users (all users regardless of status)
# test_user_report_active_only (active users only)
# test_user_report_inactive_only (inactive users only)
# test_user_report_contains_required_fields (has all required fields)
# test_user_report_invalid (error handling / tests for bad input)
